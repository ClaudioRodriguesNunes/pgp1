<resources>
    <!-- Tema global do app -->
    <style name="Theme.PGP1Resgate" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Ajustes básicos de barra de status/nav (opcional) -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>
</resources>
